# Audit Remediation Tracker — 2026-05-09

Live working document for the 21 issues surfaced by the 2026-05-09 full diagnostic. Each issue gets a deep-dive verification (so I can correct the initial assessment if wrong), a plan, and an outcome record. Items are processed in priority order (Critical → High → Medium → Low).

**Status legend:** ⏳ pending · 🔍 deep-dive in progress · 🛠️ fixing · ✅ verified · ❌ confirmed not a bug (with reason)

---

## CRITICAL

### Issue 1 — Aggregate "All Profiles" view shows only the LAST profile's metrics
**Status:** 🔍 deep-dive complete; awaiting greenlight
**Source:** page audit; `views.py:2141-2173` (/performance) and `views.py:3050-3082` (/ai)

**Deep-dive verification (2026-05-09):**
- ✅ Confirmed: query block at views.py:2141 (and identical block at :3050) sits **outside** the `for db_path in db_paths` loop (loop body ends at lines 2128-2129 / 3038-3039). Uses leftover `db_path` variable from the last loop iteration.
- ✅ Confirmed: gated on `if ai_perf["hold_resolved"] > 0:` (line 2135 / 3045). If no HOLDs across any profile, query is skipped entirely; win_rate/calibration/returns stay at 0.0 (dict init defaults at lines 2052-2074 / 2960-2986).
- 🟡 **Real-world impact is non-deterministic, not "always the last":** `db_paths = set()` (line 2915) — Python sets don't preserve iteration order. So whichever profile ends up last in the set iteration contributes to the metrics. Could change between page loads.
- ✅ Confirmed: `get_ai_performance()` in `ai_tracker.py:534` already returns per-profile `win_rate`, `avg_confidence_on_wins/losses`, `avg_return_on_buys/sells`. The aggregate routes' loop body (views.py:2086-2127) just doesn't aggregate these fields — it aggregates the wins/resolved/best/worst counts but skips the rate/calibration fields, then runs the buggy second query to fill them.

**Affected fields** (on /performance + /ai):
- `win_rate`
- `avg_confidence_on_wins`, `avg_confidence_on_losses`
- `n_buys`, `n_sells`
- `avg_return_on_buys`, `avg_return_on_sells`
- `n_directional_long`, `n_directional_short`, `n_exit_long`, `n_exit_short` + their `avg_return_on_*` siblings

**NOT affected:**
- Single-profile view (set has only 1 element → loop iterates once → "leftover" db_path is the right one)
- Aggregate fields that ARE accumulated in the loop: total_predictions, resolved, pending, directional_resolved, directional_wins, directional_win_rate, hold_resolved, hold_pass_rate, best_trade, worst_trade, biggest_missed_gain, biggest_avoided_loss
- profit_factor (a separate query at views.py:2196 / :3107 — already correctly inside its own per-profile loop; Issue 4 about WHERE-clause is a different problem)

**Plan:**
- Move the query block (currently lines 2141-2173 / 3050-3082) **inside** the `for db_path in db_paths` loop in both routes.
- Remove the `if ai_perf["hold_resolved"] > 0:` gating around it. The query collects ALL resolved predictions (`WHERE status = 'resolved'`); none of the downstream stats depend on whether HOLDs exist.
- Keep the gating around the hold_pass_rate calc (which IS correctly hold-dependent).
- Add a regression test: mock 2 profile DBs with different resolved predictions; assert aggregate `win_rate` / `avg_confidence_on_wins` / `n_buys` are the SUM/weighted-avg, not just one profile's.
- Add a guardrail test: walk every place that loops over `db_paths` and assert the per-DB SQL queries are inside the loop, not outside (AST scan).
- Risks: could expose existing bugs in single-profile-view tests if any test was unknowingly relying on the leftover-db_path behavior — full suite check will catch.

**Outcome (2026-05-09):**
- ✅ Commit `5432ae1`. Both routes fixed: query block moved inside the `for db_path in db_paths` loop in `/performance` and `/ai`. Misplaced `if hold_resolved > 0:` gate removed. Silent `except: pass` upgraded to WARNING with the failing db_path so future DB issues surface in journald.
- ✅ Cross-cutting AST guardrail added (`tests/test_aggregate_per_profile_loops.py::test_no_sqlite_connect_db_path_outside_for_loop`). Restricted to functions that have `for db_path in X:` so single-profile helpers don't false-positive. Iteration-1 of the test surfaced a false positive at line 1929 (`performance_dashboard`'s per-profile loop) where `db_path` is rebuilt fresh from `p['id']` — refined the guardrail to recognize body-level rebinding.
- ✅ 3 behavioral tests pin the fix: two-profile aggregate sums to N+M (not just N or M), set iteration order doesn't change the result across 3 runs, single-profile view unchanged.
- ✅ Live verify: web restarted 17:48:16 UTC. **0 exceptions on /performance, /ai, or /api/dashboard-totals since restart.** **0 ai-performance / ai-dashboard per-DB aggregation warnings** (every profile DB queried successfully).
- ✅ User-visible behavior change: aggregate "All Profiles" view metrics will now reflect the SUM across every profile, not a random single profile per page load. Single-profile views unchanged.
- ✅ No new issues introduced: 2,234 tests pass, full suite green, no scheduler errors, no new error patterns in journald.

---

### Issue 2 — Admin "API Calls Today" identical for every user row
**Status:** 🛠️ in progress
**Source:** page audit; `views.py:4480-4491` (actual lines `4491-4502` after re-read)
**Reported:** the route sums `total_calls` / `total_cost` across every `quantopsai_profile_*.db` and assigns the same total to every user row. No per-user attribution. Privacy/attribution issue if a real second user is ever onboarded.

**Deep-dive verification (2026-05-09):**
- Confirmed at `views.py:4491-4502`. Exact code:
  ```python
  for f in glob.glob("quantopsai_profile_*.db"):
      s = spend_summary(f)
      total_calls += s["today"]["calls"]
      total_cost  += s["today"]["usd"]
  for u in users:
      u["api_calls_today"] = total_calls
      u["api_cost_today"]  = round(total_cost, 2)
  ```
  `glob` returns ALL profile DBs system-wide; the same total is then stamped onto every user row.
- Currently the only logged-in user is `mack@…` (admin), so the **today's user-visible damage is zero** — there's no second user to mis-attribute to. But:
  - The moment a second user account is added, the admin panel will show *the admin's* API costs against that second user (and vice versa) → privacy / attribution leak.
  - The total shown is also wrong as a "per-user" number even today: it's the system aggregate, not "how much has this user spent today."
- Schema confirms a clean per-user attribution path:
  - `trading_profiles.user_id` exists (`models.py:142`).
  - Per-profile DB filename is `quantopsai_profile_<profile_id>.db` (matches `glob` pattern).
  - Helper `models.get_user_profiles(user_id)` already returns *all* profiles for a user (enabled + disabled). Disabled profiles still have a DB on disk and their historical cost should still count toward "today" if they ran earlier in the day before being toggled off.
  - `spend_summary(db_path)` already returns zeros if the DB file is missing (`ai_cost_ledger.py:73-77`), so no extra existence check needed.
- Template `templates/admin.html:32` reads `{{ u.api_calls_today }}`. No template change required — just the data needs to be per-user.

**Plan:**
- Replace the system-wide glob in the admin route with a per-user loop:
  ```python
  for u in users:
      profiles = get_user_profiles(u["id"])
      calls = cost = 0
      for p in profiles:
          db_path = f"quantopsai_profile_{p['id']}.db"
          try:
              s = spend_summary(db_path)
              calls += s["today"]["calls"]
              cost  += s["today"]["usd"]
          except Exception as exc:
              logger.warning(
                  "admin: spend_summary failed for user=%s profile=%s db=%s: %s",
                  u["id"], p["id"], db_path, exc,
              )
      u["api_calls_today"] = calls
      u["api_cost_today"]  = round(cost, 2)
  ```
- Behavioral test: stub two users with two profiles each; assert each user's row reflects ONLY their own profiles' cost; assert no leakage between users.
- Edge-case test: a user with zero profiles → row shows 0 / $0.00 (not None, not error).
- Edge-case test: a profile DB whose `spend_summary` raises → that profile contributes 0, the rest of the user's profiles still aggregate; endpoint still returns 200.
- Cross-cutting guardrail: AST-scan for `glob.glob("quantopsai_profile_*.db")` outside `ai_cost_ledger.py` / scheduler bootstrap. Any *user-attributed* surface (admin, dashboards, per-user reports) must source from `get_user_profiles(user_id)`, not a system glob. Targeted enough to allow the legitimate scheduler-side glob (which intentionally walks all profile DBs).
- Risks: none material. The new logic always returns ≤ the old system-wide total per user; tests pin no leakage; admin template unchanged. No prod deploy concerns beyond a route change.

**Outcome (2026-05-09):**
- ✅ Commit `84499aa`. `views.admin` now loops users → `get_user_profiles(u['id'])` → sums `spend_summary` per profile owned by that user. Per-profile failures log a WARNING with user/profile/db and contribute 0; the rest of the user's profiles still aggregate.
- ✅ Cross-cutting AST guardrail (`test_no_system_wide_profile_glob_in_views_py`) blocks any future `views.py` function from re-introducing `glob("quantopsai_profile_*.db")`. Allowlist is empty — must be made explicit if ever needed.
- ✅ 3 behavioral tests pin: no cross-user cost leakage (two users + three profiles), empty-profiles user shows 0, per-profile failure doesn't break others or the route.
- ✅ Live verify: web restarted 18:29:49 UTC. **0 exceptions on /admin since restart.** **0 admin spend_summary warnings** (every per-user lookup succeeded). HTTP 302 to login (as expected for unauthenticated curl).
- ✅ User-visible behavior change: each row in /admin's user table now shows that user's actual API spend today, not the system-wide aggregate stamped uniformly.
- ✅ No new issues introduced: 2,507 tests pass (4 new), full suite green.

---

### Issue 3 — `/api/dashboard-totals` 500s; happy-accident hides it; fixing one half alone would break the dashboard
**Status:** 🛠️ in progress
**Source:** page audit; `views.py:4761` and `:4775`

**Deep-dive verification (2026-05-09):**
- ✅ The audit's claim that `get_today_total` doesn't exist in `ai_cost_ledger.py` is correct (only `log_ai_call` and `spend_summary`). Reproduced via direct python.
- ❌ The audit's headline "permanently $0" was **wrong**. Live `journalctl -u quantopsai-web` shows the endpoint actually 500s every 5 minutes with `ImportError: cannot import name 'build_context' from 'user_context'` at `views.py:4761` — BEFORE control ever reaches the `get_today_total` import.
- The JS at `dashboard.html:181-194` reads `d.error` and returns early; nothing gets overwritten. Server-rendered initial values (which use the correct `spend_summary(...)["today"]["usd"]` path at `views.py:411` and the jinja footer at `dashboard.html:157`) stay on screen all day. **User has been seeing correct values precisely because the endpoint is broken.**
- Risk of fixing only one of the two broken imports: endpoint would start returning `{cost_today: 0}` for every profile, JS would start writing `$0` over the correct cells. **Must fix both together** — exactly the regression the user warned against.

**Plan:**
- Fix both broken imports in `views.api_dashboard_totals`:
  - `from user_context import build_context` → `from models import build_user_context_from_profile` (line 4761)
  - `from ai_cost_ledger import get_today_total; cost_today = float(get_today_total(...))` → `from ai_cost_ledger import spend_summary; cost_today = float(spend_summary(ctx.db_path)["today"]["usd"] or 0)` (lines 4775-4776)
- Add regression test that mocks `spend_summary` + `build_user_context_from_profile`, calls the endpoint, asserts the JSON returns the mocked cost value (not 0).
- Verify on prod: hit the endpoint with auth, confirm 200 + non-zero cost; tail journald for 5 min and confirm the 500s stop.

**Outcome (2026-05-09):**
- ✅ Commit `7d37cbf`. Three broken imports fixed together (test caught a hidden NameError on `get_account_info`/`get_positions` that the original ImportError had been masking — these were never imported in the function body).
- ✅ Per-profile silent-skip log upgraded from `debug` to WARNING with profile id + exc — surfaces any future failure in journald instead of hiding.
- ✅ 2 new tests in `tests/test_api_dashboard_totals.py` pinning the JSON shape and the spend_summary-failure path. 2,230 total pass.
- ✅ Live verify: web service restarted 17:33:47 UTC; **0 `Exception on /api/dashboard-totals` errors since restart** (was firing every 5 min for weeks). Endpoint now returns real cost values; JS auto-refresh now updates per-profile cells with the same correct value the page-load showed.
- ✅ User-visible behavior unchanged: dashboard still shows correct cost values. Footer total + per-profile cells stay correct. The difference is now they stay correct because the data is real, not because the JS is silently failing.
- ✅ No new issues introduced: full suite green, scheduler healthy, no new error patterns in journald.

---

### Issue 4 — Profit factor calc excludes most signal types
**Status:** 🛠️ in progress
**Source:** page audit; `views.py:2196` and `:3107` (actual lines `2200-2218` and `3114-3130`)
**Reported:** queries restrict to `predicted_signal IN ('BUY','SELL')` — excluding `STRONG_BUY`, `STRONG_SELL`, `SHORT`, `STRONG_SHORT`, `MULTILEG_OPEN`, `PAIR_TRADE`. Headline "Prediction Accuracy" / "Profit Factor" reflects a small subset of trades.

**Deep-dive verification (2026-05-09):**
- Confirmed in both `/performance` (views.py:2200-2218) and `/ai` (views.py:3114-3130). Identical bug shape — same `predicted_signal IN ('BUY', 'SELL')` whitelist:
  ```python
  rows = conn.execute(
      "SELECT actual_return_pct FROM ai_predictions "
      "WHERE status='resolved' AND actual_return_pct IS NOT NULL "
      "AND predicted_signal IN ('BUY', 'SELL')"
  ).fetchall()
  ```
- **Real-world impact (production data, 11 profiles):**
  | Signal | Resolved+returns | Counted? |
  |--------|-----|---|
  | BUY    | 785 | ✅ |
  | SELL   | 3   | ✅ |
  | SHORT  | 25  | ❌ |
  | MULTILEG_OPEN | 113 | ❌ |
  | HOLD   | ~13,500 | ❌ (correct — no trade) |

  → **138 of 926 actually-traded predictions (~15%) are silently dropped from profit_factor.** The headline number is computed on a subset; SHORT positions and MULTILEG_OPEN options never count toward it.
- The dropped types are *not* hypotheticals. SHORT lives on enabled small-cap-shorts profiles; MULTILEG_OPEN is the dominant signal type for all options profiles. Profit factor on options profiles is essentially meaningless today (only stock BUYs count).
- Audit also lists `STRONG_BUY/STRONG_SELL/STRONG_SHORT/PAIR_TRADE` — none of these appear in any current prod DB. Don't whitelist phantom types; just stop whitelisting.
- Convention check: `ai_tracker.py:614,620,723,730` already uses `UPPER(predicted_signal) = 'HOLD'` as the canonical "no-trade" filter. So the project-wide convention is **HOLD = no trade, everything else = trade**. The profit_factor query just doesn't follow it.

**Why missed:** the IN(...) whitelist was correct when only BUY/SELL existed. New signal types (SHORT, MULTILEG_OPEN) were added to the prediction recorder but the downstream consumer queries were never re-audited. Classic whitelist-rot. (Issue 1 was a structural mistake, this is the same lesson — **don't whitelist closed sets when an open-world exclusion expresses the intent.**)

**Plan:**
- Replace whitelist with HOLD-exclusion in both routes:
  ```python
  "WHERE status='resolved' AND actual_return_pct IS NOT NULL "
  "AND predicted_signal IS NOT NULL "
  "AND UPPER(predicted_signal) != 'HOLD'"
  ```
  This matches the established convention (ai_tracker.py uses UPPER(...)='HOLD' for the inverse filter).
- Behavioral test: seed a DB with one of each {BUY, SELL, SHORT, MULTILEG_OPEN, HOLD, NULL} predictions with known returns; assert profit_factor includes BUY+SELL+SHORT+MULTILEG_OPEN, excludes HOLD and NULL, and matches hand-computed value.
- Future-proofing test: assert any signal type other than HOLD/NULL is included in profit_factor (covers `STRONG_BUY`, `PAIR_TRADE`, `EXIT`, `COVER`, etc. if they ever ship).
- Cross-cutting AST guardrail: scan `views.py` for any SQL string containing `predicted_signal IN (` — fail the test, since this is the exact bug shape. Allowlist if a future query legitimately needs to target a closed set (force explicit decision).
- Risks: profit_factor will move on user-visible dashboards. Direction: usually toward more accurate (more trades counted, less luck of subset). On options profiles it could move dramatically (currently profit_factor reflects ~10% of trades; will reflect 100% post-fix). This is the correct direction — the displayed number was structurally wrong, not noisy.

**Outcome (2026-05-09):**
- ✅ Commit `c9598ff`. Both routes use `UPPER(predicted_signal) != 'HOLD'` (matching the existing `ai_tracker.py` convention). Silent `except: pass` upgraded to WARNING with failing db_path.
- ✅ Cross-cutting AST guardrail (`test_no_predicted_signal_in_whitelist_in_views_py`) blocks any `predicted_signal IN (...)` whitelist in `views.py`. Empty allowlist.
- ✅ 5 behavioral tests pin: every prod-real signal type included, HOLD case-insensitive exclusion, NULL signal exclusion, future-signal-type inclusion property, all-HOLD edge case.
- ✅ Live verify: web restarted 18:41:29 UTC. **0 exceptions on /performance or /ai since restart.** **0 profit_factor query warnings.**
- ✅ **Real prod numerical proof on largest options profile (id=3):**
  - OLD whitelist: profit_factor = 1.16 over 72 trades (BUY/SELL only)
  - NEW HOLD-exclude: profit_factor = **0.95 over 87 trades** (adds 15 SHORT+MULTILEG_OPEN trades)
  - The displayed value moved from 1.16 (ostensibly "profitable") to 0.95 (net losing) — the hidden SHORT/MULTILEG_OPEN trades have been net losers on this profile and were being silently hidden from the headline metric. **The user now sees the truth.**
- ✅ No new issues: 2,513 tests pass (6 new), full suite green.

---

## HIGH

### Issue 5 — Recent Activity table renders raw snake_case
**Status:** ✅ resolved
**Source:** page audit; `dashboard.html:269-278`
**Reported:** `d.action_taken` / `d.decision_type` not piped through humanize. Values like `STRONG_BUY` / `bull_put_spread` render raw — same class as the `insufficient_history` slippage leak.

**Deep-dive verification (2026-05-09):**
- Confirmed `dashboard.html:274-275`. Cross-cutting search found 5 more instances of the same bug class on other pages:
  - `trades.html:45` (`d.decision_type`), `:49` (`d.ai_signal`), `:54` (`d.action_taken`)
  - `ai_strategy.html:35` (`v.market_type`)
  - `ai.html:499` (`v.market_type`)
  - `dashboard.html:330` (`prof.account.status`) — Alpaca returns ACCOUNT_RESTRICTED etc. as UPPER_SNAKE
- All 7 cells render dynamic values that come from the LLM (decision_type, ai_signal), the prediction recorder (action_taken), the market_type slug system (small_cap_shorts, options_earnings), or third-party APIs (Alpaca status). humanize is registered as a Jinja filter (`display_names.py:627`) and is idempotent.
- **Side finding (separate issue, see Issue 22 below):** `decision_log` table in prod is empty; `log_decision()` writer exists but **no production code calls it**. The Recent Activity panel has been silently empty for an unknown duration. Issue 5's rendering fix still matters (the panel will populate the moment the writer is wired up), but the missing-writer is the bigger bug.

**Plan:** pipe each cell through `| humanize` (idempotent — safe to apply broadly). Add cross-cutting Jinja-template guardrail that scans for known dynamic snake_case-likely fields rendered without a humanizing filter.

**Outcome (2026-05-09):**
- ✅ Commit (pending): 7 cells fixed across 4 templates — `dashboard.html:274,275,330`, `trades.html:45,49,54`, `ai_strategy.html:35`, `ai.html:499`.
- ✅ Cross-cutting guardrail (`test_no_raw_render_of_dynamic_snake_case_fields`) scans every `templates/*.html` for `{{ ... .<dynamic_field> }}` renders without `| humanize`/`| display_name`/`| title`. Knows to skip predicate uses (`if 'BUY' in d.ai_signal`) and HTML attribute slugs (`action="…/{{ d.strategy_type }}"`). Allowlist of dynamic fields is closed/explicit — adding one is the enforcement point.
- ✅ 5 behavioral tests pin the per-field rendering: STRONG_BUY → "Strong Buy", bull_put_spread → "Bull Put Spread", MULTILEG_OPEN → "Multileg Open", small_cap_shorts → "Small Cap Shorts", ACCOUNT_RESTRICTED → "Account Restricted".
- ✅ The guardrail surfaced a false negative the audit missed: `ai.html:664` had a stray `{{ d.strategy_type }}` (raw) in URL slug context — properly skipped by the new HTML-attr-slug heuristic.
- ✅ No prod numerical change today (Recent Activity panel is empty due to missing writer); fix is forward-looking.
**Outcome:** _pending_

---

### Issue 6 — `_task_intraday_risk_check` missing two of four inputs
**Status:** 🛠️ in progress
**Source:** TODO audit; `multi_scheduler.py:1324`
**Reported:** comment `# sector_moves + halted_held_symbols deferred`. The intraday risk monitor accepts these args; the scheduler still passes nothing. Sector-swing and held-position-halted alerts silently inactive.

**Deep-dive verification (2026-05-09):**
- Confirmed at `multi_scheduler.py:1319-1325`: `collect_intraday_alerts()` is called with only `today_intraday_pct`, `avg_7d_intraday_pct`, `current_hourly_vol`, `avg_20d_hourly_vol`. Inline comment: `# sector_moves + halted_held_symbols deferred`.
- The two unwired check functions are FULLY IMPLEMENTED in `intraday_risk_monitor.py`:
  - `check_sector_concentration_swing(sector_moves)` at line 125 — fires when any sector's intraday move exceeds `SECTOR_SWING_PCT`. Threshold-bucketed: warning if ≥ threshold, critical if ≥ 5.0%.
  - `check_held_position_halts(halted_held_symbols)` at line 157 — fires when any held position is halted. Severity scales with count (warning < 3, critical ≥ 3).
- Both have unit tests in `tests/test_intraday_risk_monitor.py`. They're tested but never invoked with non-empty data in production.
- **Real-world impact:** Two of four legs of the safety system are dark. If a sector swings 5% intraday (e.g., financials selloff like SVB-day) the system continues approving new entries. If a held position gets halted (delisting / trading-halt event), the system doesn't react. These are exactly the kinds of events the intraday risk monitor was built for; the safety net has 50% coverage.
- **Available data sources, no new external calls needed:**
  - sector_moves → `market_data.SECTOR_ETFS` defines the 11 SPDR ETFs (XLK/XLF/XLE/XLV/XLI/XLY/XLP/XLU/XLB/XLRE/XLC). `get_bars(etf, limit=2)` gives today + yesterday daily bars; `(today_close - yesterday_close) / yesterday_close` is the signed intraday move. Alpaca returns today's partial bar during market hours.
  - halted_held_symbols → `client.get_positions(ctx=ctx)` already returns held symbols. Alpaca's `api.get_asset(symbol).tradable` is `False` for halted/restricted/delisted assets (already used in `client.py:318` for shortable-check). Conservative: a symbol counts as "halted" iff `tradable=False`.

**Plan:**
1. Refactor `_task_intraday_risk_check` to fetch the two missing inputs:
   - `_compute_sector_moves()` helper: iterate `SECTOR_ETFS`, fetch 2-bar history per ETF, compute pct change. Skip any ETF whose data is missing (don't fire false alerts).
   - `_compute_halted_held_symbols(ctx)` helper: walk `get_positions(ctx=ctx)`, hit `api.get_asset(symbol).tradable` for each, return list of non-tradable symbols. Cache aggressively (15-min) to avoid hitting Alpaca every cycle.
2. Pass both into `collect_intraday_alerts(...)`.
3. Remove the `# deferred` comment.
4. Behavioral test: seed both inputs with alert-triggering values, assert all 4 checks fire and `aggregate_action` returns the right severity.
5. **Cross-cutting AST guardrail:** any call to `collect_intraday_alerts(...)` in `multi_scheduler.py` must pass ALL named arguments the function accepts (uses `inspect.signature` to enumerate). Catches the meta-pattern: "scheduler quietly drops a parameter and silently disables a check." Empty allowlist.
- Risks: false-positive halts could block legitimate trading. Mitigations: conservative tradable check, 15-min cache, severity bucketing already in the check functions, and the existing `aggregate_action` requires `pause_all` from a critical alert (warnings only `block_new_entries`, which is recoverable). Halt-status fetch failures must NOT fire an alert (return [] on exception, log warning).

**Outcome (2026-05-09):**
- ✅ Commit `e84296d`. Two new helpers in `multi_scheduler.py`: `_compute_sector_moves()` (iterates `SECTOR_ETFS`, computes signed pct from 2-bar history per ETF; missing data silently omitted) and `_compute_halted_held_symbols(ctx)` (walks held positions, checks `api.get_asset(...).tradable`, 15-min cache). Both passed into `collect_intraday_alerts(...)`. `# deferred` comment removed.
- ✅ Cross-cutting AST guardrail (`test_scheduler_passes_all_collect_intraday_alerts_args`) uses `inspect.signature(collect_intraday_alerts)` to enumerate every parameter, then AST-scans the call site asserting every param appears as a kwarg. Catches any future "deferred" silencing of a check. Empty allowlist.
- ✅ 5 behavioral tests pin: signed pct correctness for sector moves, missing-data omission (no false alerts), zero-divide safety, non-tradable symbol detection, get_asset failure isolation, 15-min cache effectiveness.
- ✅ Live deploy verified: scheduler restarted 03:48:06 UTC. **0 errors, 0 intraday_risk warnings since restart.** Scheduler is in market-closed sleep until Mon 09:30 EDT — first live execution of the now-fully-wired intraday risk task happens at next scan cycle Monday morning.
- ✅ No prod numerical change today (market closed Sunday). Behavioral effect arrives at Monday open: alerts will now fire on sector swings ≥ 2% and on any non-tradable held position.
- ✅ No new issues: 2,526 tests pass (6 new), full suite green.

---

### Issue 7 — `options_earnings_plays.py` macro hook never wired
**Status:** 🛠️ in progress
**Source:** TODO audit; `options_earnings_plays.py:25`
**Reported:** OPEN_ITEMS marks `evaluate_macro_play` as shipped 2026-05-03, but `options_earnings_plays.py` still says "deferred until macro-event tracker exists" and doesn't call it. Prerequisite shipped, integration didn't.

**Deep-dive verification (2026-05-09):**
- Confirmed at `options_earnings_plays.py:24-25` — the docstring still says: *"Macro events (FOMC, CPI, NFP) follow the same crush-capture logic with index ETFs (SPY/QQQ); deferred until macro-event tracker exists."*
- The macro-event tracker **exists in full**: `macro_event_tracker.py` ships `MACRO_EVENT_CALENDAR` (FOMC/CPI/NFP through end of 2026), `get_upcoming_macro_event()`, `days_until_next_event()`, `evaluate_macro_play(*, iv_rank_pct, days_until_event, event, spy_price)`, and `render_macro_event_for_prompt()`. Module is the index-ETF analog of `options_earnings_plays.py`.
- **`render_macro_event_for_prompt()` IS wired** (used at `trade_pipeline.py:2955-2956`) — but it's just a heads-up line ("Next macro event: FOMC tomorrow"). It does NOT produce a BUY/SELL recommendation.
- **`evaluate_macro_play()` IS NOT called anywhere** — verified via grep across all production code. The function that produces the actual SELL-iron-condor / BUY-straddle recommendation has zero callers.
- The earnings analog fully implements the prompt-block integration: `render_earnings_plays_for_prompt()` is wired at `ai_analyst.py:741-758` and emits an "EARNINGS PLAYS" block with per-symbol recommendations. **There is NO equivalent macro-plays render function** — the missing piece is the SPY-level wrapper that pulls together the 4 inputs (event, days, IV rank, price) and calls `evaluate_macro_play`.
- **OPEN_ITEMS contradicts itself:** line 80 says Phase F2 is `⏳ OPEN`; line 195 still calls out the "deferred" comment as open; line 231 says `✅ Macro event tracker (Phase F2)`. CHANGELOG line 951 claims "Closes Phase F2 of OPTIONS_PROGRAM_PLAN" — but that's only true for the tracker, not the integration.
- **Real-world impact:** every FOMC / CPI / NFP day, the AI prompt currently shows a heads-up ("FOMC tomorrow") but no actionable recommendation. A pre-event SPY iron condor with rich IV (the canonical macro-IV-crush play) is never proposed by the system. Today (2026-05-10) the next FOMC is 2026-06-18 (~39 days out, outside the 5-day pre-window) — so no near-term concrete miss; but the system has been silently missing every macro play opportunity since shipping the tracker.

**Plan:**
1. Add `render_macro_play_recommendation_for_prompt(get_iv_rank, get_spy_price)` to `macro_event_tracker.py`. Mirrors the earnings analog's contract: pulls the 4 inputs (event from `get_upcoming_macro_event`, days from `days_until_next_event`, SPY IV rank from injected lookup, SPY price from injected lookup), calls `evaluate_macro_play`, returns formatted prompt block or `""`. Inject lookups so the function stays unit-testable.
2. Wire it in `trade_pipeline.py` next to the existing `render_macro_event_for_prompt()` call. Implement the lookup callables there so they live with the rest of the prompt-building plumbing.
3. Remove the stale "deferred" wording from `options_earnings_plays.py:24-25`. Replace with a forward-pointer comment that says where the macro analog lives.
4. Update `OPEN_ITEMS.md` lines 80 + 195 to mark resolved (closing the contradiction with line 231).
5. Behavioral tests: stub the 4 inputs to trigger each branch (rich IV → iron condor recommendation; cheap IV → straddle; outside-window → empty); assert prompt block contains the rationale text from `evaluate_macro_play`.
6. Pin a regression-style test asserting `evaluate_macro_play` has at least one production caller (analog to `test_no_unwired_writers` for `models.py` SQL writers, but scoped to this specific function — narrow guard, not a sweeping new pattern).

**Outcome (2026-05-09):**
- ✅ Commit `5020ad5`. New `render_macro_play_recommendation_for_prompt(*, iv_rank_lookup, spy_price_lookup)` in `macro_event_tracker.py` mirrors the earnings analog. Wired in `trade_pipeline.py` next to the existing `render_macro_event_for_prompt()` call. New `macro_play_block` field flows through `market_context` to `ai_analyst.py:961` for the prompt.
- ✅ Stale `options_earnings_plays.py:24-25` docstring rewritten to point at the macro analog (instead of claiming it doesn't exist).
- ✅ `OPEN_ITEMS.md` lines 80, 194 (Issue 6 stale entry caught while in this file), and 195 marked DONE with shipped-date.
- ✅ 7 behavioral tests + 1 narrow regression-guard pin: every branch of `evaluate_macro_play` triggers correctly through the wrapper; broken IV/price lookups NEVER produce a play (safety rule); `render_macro_play_recommendation_for_prompt` must have at least one production caller.
- ✅ Live deploy verified: scheduler 04:02:32 UTC, web 04:02:32 UTC. **0 errors / ImportError / NameError on either service since restart.** Wiring confirmed on disk on prod.
- ✅ No prod numerical change today (next FOMC is 2026-06-18, ~39 days out — outside the 5-day pre-window). First live macro-play recommendation will be emitted into the AI prompt around 2026-06-13 (5 days before FOMC) IF SPY's IV rank is rich (>= 75) or cheap (<= 25) at that time. The system will no longer silently miss the canonical IV-crush play.
- ✅ No new issues: 2,534 tests pass (8 new), full suite green.

---

### Issue 8 — /performance computes tuning_history / learned_patterns / sec_alerts then throws them away
**Status:** 🛠️ in progress
**Source:** page audit; `views.py:2236-2305`, `:2536-2564` compute; `:2635-2641` `render_template` passes empty lists.
**Reported:** Real work done, dead-ended. UI blocks expecting these show nothing.

**Deep-dive verification (2026-05-09):**
- Confirmed at `views.py:2593-2619`. Four kwargs in the `render_template("performance.html", ...)` call are passed as **hardcoded literal empty lists** while the same-named variables are populated with real work earlier in the function body:
  - `tuning_history` — populated 2208-2225 (per-profile `get_tuning_history` with formatted labels) → passed `tuning_history=[]` at 2607.
  - `tuning_status` — populated 2240-2275 (per-profile self-tuning state, last-run timestamp) → passed `tuning_status=[]` at 2608.
  - `learned_patterns` — populated 2228-2235 (per-DB `_analyze_failure_patterns`) → passed `learned_patterns=[]` at 2609.
  - `sec_alerts` — populated 2509-2536 (per-profile SEC filing alerts, sorted by severity + date) → passed `sec_alerts=[]` at 2613.
- **Audit claim correction:** the audit said "UI blocks expecting these show nothing." That's not quite right — `templates/performance.html` doesn't reference any of these four variables (verified via grep). The data has BOTH a dead writer in this route AND no consumer in this template.
- The variables ARE consumed by `templates/ai_performance.html` (e.g. `tuning_history` at line 467), which is rendered from a DIFFERENT route (`views.api_performance` at `views.py:1795`). That route correctly passes `tuning_history=tuning_history[:20]`. So the bug here is purely waste — CPU + DB queries on every /performance page load with the result discarded — not user-visible missing data.
- Cost of the wasted work: 4 datasets recomputed per /performance page load. `tuning_history` does an N-profile Python loop with DB queries; `learned_patterns` calls `_analyze_failure_patterns` per DB; `tuning_status` builds a `UserContext` per profile + runs `describe_tuning_state`; `sec_alerts` is the most expensive — opens a SQLite connection per profile and calls `get_active_alerts`. None are bounded by request-time caching at this layer.

**Why the bug exists:** my best guess from the shape — these were copied from `views.api_performance` (the correct usage at line 1795-1804) when someone scaffolded the institutional dashboard, but the dashboard's panels for these never got designed/added. The empty-list args were placeholder overrides that hid the partially-shipped state.

**Plan:**
1. Delete the four dead computations entirely (lines ~2208-2235 for tuning_history + learned_patterns; ~2240-2277 for tuning_status; ~2509-2536 for sec_alerts). Each block is preceded by a section comment that gets removed too.
2. Delete the four dead `=[]` kwargs in the `render_template` call.
3. Cross-cutting AST guardrail: scan `views.py` for any `render_template(...)` call where a kwarg's value is a literal `[]` or `{}` AND a same-named variable was assigned non-empty content earlier in the function. That's the exact "computed-then-thrown-away" shape. Empty-list defaults that were never populated are legit; this targets the dead-throw specifically.
- Risks: minimal. No template consumes these from the /performance route. Saves ~4-N DB queries + 1 UserContext build per /performance refresh. Tests already cover the routes that DO use these (api_performance via ai_performance.html).

**Outcome (2026-05-09 — THREE-COMMIT DETOUR ending in correct state):**
- ⚠️ **Commit `52f4cc5`** — deleted the 4 orphaned computations. Action correct, rationale incomplete. I framed it as "no template consumes them" without checking if the same panels exist elsewhere.
- ⚠️ **Commit `0090d2b`** — Mack pushed back framing the delete as a lazy fix (under the assumption the panels were missing on /performance); I built 4 duplicate UI panels on `/performance#tuning`. **WRONG** — every one of those panels already exists on `/ai#operations` as richer JS-loaded paginated widgets backed by `/api/tuning-status`, `/api/active-lessons`, and `/api/sec-alerts`. I never checked /ai. Built a duplicate UI surface 2 clicks away from the original.
- ✅ **Commit `1e0abf5`** — reverted `0090d2b`. State returns to post-`52f4cc5`: dead-throw deleted, AST guardrail in place, `/ai#operations` unchanged (the canonical home for these panels).
- ✅ Cross-cutting AST guardrail (`tests/test_no_dead_throw_render_kwargs.py`) remains. Future dead-throws fail the test.
- ✅ User-visible behavior: **none.** /performance unchanged for the user. /ai#operations continues to serve the 4 widgets as it always did.
- ✅ Backend behavior: ~4-N DB queries + 1 UserContext-build saved per /performance refresh.
- ✅ No new issues introduced: full suite green.

**Two new feedback memories** capture the meta-lesson (so this doesn't happen a third time):
- `feedback_never_delete_as_shortcut.md` — partially-shipped features must be COMPLETED, not deleted (default rule).
- `feedback_check_existing_before_acting.md` — **but** before judging delete-vs-build, exhaustively grep the codebase (templates AND JS AND API endpoints AND sibling routes) for the same functionality. The fundamental rule: investigate the WHOLE landscape before acting on a single point. Both `52f4cc5` and `0090d2b` violated this.

---

### Issue 9 — Bare `except: pass` is ubiquitous in views.py
**Status:** ⏳ pending
**Source:** page audit; ~30 instances across `views.py`
**Reported:** Each is a potential silent failure (the Issue 3 ImportError swallow is one example). Structural violation of the no-silent-failures rule. The existing guardrail `test_no_bare_except_pass_on_db_or_broker_calls` covers trade-execution modules but not views.py.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

## MEDIUM

### Issue 10 — Stale comments in 4 modules contradict shipped code
**Status:** 🛠️ in progress
**Source:** TODO audit
**Reported:**
- `mc_backtest.py:21-25` — Limits docstring claims by-day bootstrap is "future enhancement"; shipped 2026-05-03 and is the default.
- `alternative_data.py:8-10` — module docstring says intraday 5-min bars "not yet migrated"; migration completed 2026-05-01.
- `news_sentiment.py:226-235` — `fetch_news_yfinance` labeled "DEPRECATED" + "yfinance fallback"; body is now `return fetch_news_alpaca(...)`. Pure alias.
- `slippage_model.py:165` — comment "We don't store ADV at trade time" contradicts the next 30 lines that read `adv_at_decision FROM trades`.

**Deep-dive verification (2026-05-10):** all 4 claims confirmed.

1. **`mc_backtest.py:21-25`** — Limits docstring: *"correlated regimes (a full day of wide spreads when the whole strategy fires) aren't captured. To capture those, we'd need to bootstrap by day, not by trade — future enhancement."* Verified: `bootstrap_mode='by_day'` is the **default** (line 128), implemented at lines 135/152/156/194/209. The "future enhancement" framing is stale. Comment now misleads readers about what the module actually does.

2. **`alternative_data.py:8-10`** — Module docstring: *"Intraday 5-min bars (used in `get_intraday_microstructure`): SHOULD migrate to Alpaca `/v2/stocks/{sym}/bars?timeframe=5Min`. Tracked as a known follow-up; not yet migrated."* Verified: lines 372/389/395 already call the Alpaca endpoint with `timeframe=5Min`. Migration shipped (likely commit `bc0a8c0` "Migrate news/regime/political/factor/models/backtester from yfinance to Alpaca"). Docstring now describes a state that doesn't exist.

3. **`news_sentiment.py:226-235`** — Header: *"Legacy yfinance fallback... Will be removed in a future cleanup once all callers point at fetch_news_alpaca."* Docstring of `fetch_news_yfinance`: *"DEPRECATED: prefer fetch_news_alpaca. yfinance is 15+ min delayed and incomplete; kept only for fallback safety."* Verified: function body is **literally** `return fetch_news_alpaca(symbol, limit=limit)` — no yfinance call at all. Calling it "yfinance fallback" while the body delegates to Alpaca is doubly wrong.

4. **`slippage_model.py:165`** — Comment: *"We don't store ADV at trade time, so use a simple proxy: `volume_at_decision` if we have it, else fall back to a default participation."* Verified: lines 169-174 query `adv_at_decision FROM trades`; lines 199-204 read `row["adv_at_decision"]`. The column was added (per the comment at lines 199-201) but the higher-level "we don't store ADV at trade time" comment was never updated.

**Plan:** rewrite each comment to reflect current shipped state. No code changes — these are pure-docs corrections. CHANGELOG entry per the "always update changelog" rule.

For `news_sentiment.fetch_news_yfinance`: stronger move — since the function is now a pure alias to `fetch_news_alpaca`, the right fix isn't just to update the comment. It's to **delete the alias entirely** if it has no callers, OR alias it explicitly + update the docstring if callers exist. Will check callers first and decide.

**Outcome (2026-05-10):**
- ✅ `mc_backtest.py:21-29` — Limits docstring rewritten as "Bootstrap modes" describing both `by_day` (default, shipped 2026-05-03) and `per_trade` (legacy). Reflects current shipped state.
- ✅ `alternative_data.py:8-11` — module docstring rewritten to say the 5-min Alpaca bars endpoint IS the served path; "migration completed" instead of "not yet migrated".
- ✅ `news_sentiment.py:226-235` — **alias deleted entirely** (was 2 callers in `trade_pipeline.py:2631, 2814`; both updated to call `fetch_news_alpaca` directly). Stale "free from yfinance, no AI cost" comment in trade_pipeline.py:2812 also fixed to "Alpaca news endpoint, no AI cost".
- ✅ `slippage_model.py:165` — comment rewritten to describe what the SELECT actually does (uses `adv_at_decision` from trades; legacy rows fall back to the $50M ADV proxy).
- ✅ Test fixture update: `tests/test_long_short_awareness.py` had two patches targeting `kelly_sizing.compute_kelly_recommendation` and `portfolio_manager.check_drawdown` — these stopped working because Issue 9 hoisted those imports to views.py module top, so the names are bound IN views. Updated patches to target `views.compute_kelly_recommendation` / `views.check_drawdown` with a comment explaining why.
- ✅ 2,555 tests pass, full suite green.

---

### Issue 11 — OPEN_ITEMS.md §10 line numbers stale
**Status:** 🛠️ in progress
**Source:** TODO audit; `OPEN_ITEMS.md:191-199`
**Reported:** Line refs like `multi_scheduler.py:1196`, `options_roll_manager.py:32`, `slippage_model.py:165/:197` have drifted (multi_scheduler is 1324 now; slippage:197 doesn't exist; options_roll_manager:32 was already shipped per §12 #10 but still listed in §10 as ⏳ OPEN).

**Deep-dive verification (2026-05-10):** all 12 `\`file:line\`` refs in OPEN_ITEMS.md verified against current source. State of each:

| Ref | Current state | Action |
|---|---|---|
| `mc_backtest.py:25` (3 places: L64, L193, L214) | Issue 10 rewrote the comment; `bootstrap_mode='by_day'` is the default and shipped | Mark DONE; remove from §10 + §11 + the §1.2 cross-ref at L64 |
| `alternative_data.py:1928` | Stale: WoW logic is now at L2018-2096 (`_get_wow_change`, "Item 2 of OPEN_ITEMS — WoW change vs 7 days ago") — the future-enhancement claim was resolved | Mark DONE; remove from §10 |
| `multi_scheduler.py:1196` | Already DONE per Issue 6; the deferred comment was deleted; line 1196 no longer has any related content | Update to point at the new helpers `_compute_sector_moves` (L1257) / `_compute_halted_held_symbols` (L1284) for traceability |
| `options_earnings_plays.py:25` | Already DONE per Issue 7; comment rewritten | Keep DONE; line ref still points at the new comment which is fine |
| `options_roll_manager.py:32` | Comment at L32-34 says "OPEN_ITEMS #10 — these are now per-profile tunable knobs (UserContext fields, settings UI). Module constants stay as fallbacks" — per-profile tunables ARE shipped | Mark DONE |
| `slippage_model.py:165` | Issue 10 rewrote the comment; `adv_at_decision` IS used | Mark DONE |
| `slippage_model.py:197` | The "K is currently fitted from paper fills" text doesn't exist at L197 anymore — `slippage_model.py` line 197 is now a `notional = qty * dp` line. The CONCEPT (K calibrated from paper fills, recalibrate after real money) lives at L42 in the docstring | Update line ref `:197` → `:42`; keep DEFERRED status |
| `ai_analyst.py:640` | Verified: comment "the AI to propose with action='OPTIONS' (deferred to follow-up)" exists exactly at L640; not yet implemented | Keep OPEN; no change |
| `short_borrow.py:3` | Verified: docstring header L3 still says "DYNAMIC_UNIVERSE_PLAN.md / TECHNICAL_DOCUMENTATION.md §15 deferred"; not yet implemented | Keep OPEN; no change |

**Plan:**
1. Rewrite §10 with corrected line refs + DONE/OPEN states.
2. Fix the cross-references at §1.1, §1.2, §11 that point at the same stale items.
3. **Add a structural guardrail test**: scan `OPEN_ITEMS.md` for every `` `<file>:<line>` `` reference. For each ref:
   - File must exist.
   - If the entry has a quoted source-text snippet (between double-quotes in the table cell), that quote MUST appear in the file at-or-near the claimed line for ⏳ OPEN entries (because the deferred comment is supposed to still be there), AND MUST NOT appear for ✅ DONE entries (because you fixed the underlying comment).
   - Test fails on any drift. Forces the next "I rewrote comment X" change to update OPEN_ITEMS too.

**Outcome (2026-05-10):**
- ✅ §10 rewritten with corrected `file:line` refs and DONE/OPEN states. Six entries that were ⏳ OPEN are now ✅ DONE (`mc_backtest.py` by-day; `alternative_data.py` App Store WoW; `multi_scheduler.py` sector_moves+halted_held; `options_earnings_plays.py` macro analog; `options_roll_manager.py` per-profile knobs; `slippage_model.py:165` ADV proxy). Two entries kept OPEN (`ai_analyst.py:640` action='OPTIONS' surface; `short_borrow.py:3` borrow rate). One entry stayed DEFERRED but moved line ref from `:197` (which doesn't exist anymore) to `:42` (where the K-from-paper-fills concept now lives).
- ✅ Cross-references at §1.2 (L64-66) and §11 (L214-215) updated to match — the same items were referenced from multiple sections.
- ✅ NEW structural guardrail (`tests/test_open_items_refs_match_source.py`, 3 tests):
  1. `test_every_open_items_file_line_ref_resolves` — every `\`<file>:<line>\`` in OPEN_ITEMS.md must resolve to an existing file with a valid line number.
  2. `test_open_entry_quoted_text_still_exists_in_source` — for ⏳ OPEN entries with quoted source text, the quote MUST appear in the source. If it doesn't, the work shipped — mark DONE.
  3. `test_done_entry_quoted_text_no_longer_in_source` — for ✅ DONE entries quoting deferred-prose words ("deferred", "future enhancement", "we don't", etc.), the quote MUST NOT appear in source — if it still does, the comment wasn't actually rewritten.
- ✅ Verified the test catches drift: temporarily flipped one DONE entry to ⏳ OPEN — test failed correctly listing the entry. Restored.
- ✅ 2,561 tests pass.

**Memory rule applied** (`feedback_test_for_the_class_not_the_instance`): the test scans the structural pattern (every file:line ref) instead of enumerating known entries. New refs added in the future are checked by default; the developer can't forget to update OPEN_ITEMS when they rewrite a deferred comment because the build will fail.

---

### Issue 12 — 5 dead templates drift independently
**Status:** 🛠️ in progress
**Source:** page audit
**Reported:** `ai_brain.html`, `ai_strategy.html`, `ai_awareness.html`, `ai_operations.html`, `ai_performance.html` are NOT rendered (the redirect routes at `views.py:3539-3557` send to `/ai#tab` instead). They drift independently and could mislead future maintenance.

**Deep-dive verification (2026-05-10):**
- **Audit was wrong about `ai_performance.html`**: it IS rendered (verified at `views.py:1834` — the `/ai/performance` route is live and Issue 8 worked extensively in this template). NOT dead. Will keep.
- **4 of 5 ARE dead**: `ai_brain.html`, `ai_strategy.html`, `ai_awareness.html`, `ai_operations.html` have:
  - No `render_template(...)` call anywhere in views.py
  - No `{% extends %}` / `{% include %}` from any other template
  - The 4 routes (`/ai/brain`, `/ai/strategy`, `/ai/awareness`, `/ai/operations`) at `views.py:3360-3378` are pure `redirect(url_for("views.ai_dashboard") + "#<tab>")` — they never touch these templates
- **Verified deletion is safe** (per the never-delete-as-shortcut rule):
  - Every panel header in the 4 dead templates also exists in `ai.html` (the live consolidated multi-tab page): Learned Patterns, Meta-Model, Strategy Validations, Strategy Allocation, Evolving Strategy Library, Alpha Decay Monitoring, Market Intelligence, SEC Filing Alerts, Event Stream, Specialist Ensemble, Self-Tuning, AI Cost — 12/12 mapped.
  - Tokens UNIQUE to ai_awareness.html (`fred_indicators`, `etf_flows.net_flow`, `series_id`) are **broken JS references** to API fields that the API doesn't return — `tests/test_no_guessing.py:511-523` literally lists them as "made-up fields". So the dead template wouldn't even render correctly if revived.
  - One load-bearing reference: `tests/test_meta_features_have_ui.py:43` lists `templates/ai_awareness.html` in SURFACES (features-have-UI scan). Will remove from SURFACES; every feature still matches in `ai.html` so coverage doesn't shrink.

**Plan:**
1. Delete the 4 truly-dead templates (`ai_brain.html`, `ai_strategy.html`, `ai_awareness.html`, `ai_operations.html`).
2. Keep the 4 redirect routes (they preserve any external bookmarks pointing at the old URLs — the legitimate purpose of route-as-redirect).
3. Remove `templates/ai_awareness.html` from `tests/test_meta_features_have_ui.py:SURFACES`.
4. **Add a structural guardrail**: scan `templates/*.html`. Every template must be either rendered by some `render_template("name.html")` call OR extended/included by another template OR explicitly allowlisted (with a rationale). Blocks future drift — adding a new template that's never used fails the build.

**Outcome (2026-05-10):**
- ✅ Deleted **5 dead templates total**:
  - `templates/ai_brain.html` (283 lines) — content fully covered by `ai.html` Brain tab.
  - `templates/ai_strategy.html` (238 lines) — content fully covered by `ai.html` Strategy tab.
  - `templates/ai_awareness.html` (416 lines) — content covered by `ai.html` Awareness tab; uniquely contained broken JS field references already documented as "made up" in `tests/test_no_guessing.py:511-523`.
  - `templates/ai_operations.html` (215 lines) — content covered by `ai.html` Operations tab.
  - `templates/_ai_nav.html` — surfaced by the new structural guardrail; the nav partial for the dead ai_*.html templates above. No remaining references anywhere.
- ✅ Kept the 4 redirect routes (`/ai/brain` etc.) — they preserve external bookmarks pointing at old URLs.
- ✅ Removed `templates/ai_awareness.html` from `tests/test_meta_features_have_ui.py:SURFACES` (every feature still matches in `ai.html`; coverage unchanged).
- ✅ NEW structural guardrail (`tests/test_no_orphan_templates.py`): scans `templates/*.html` for orphans (no `render_template`, no `{% extends %}` / `{% include %}` / `{% import %}` / `{% from ... import %}` reference). Empty allowlist. The guardrail caught `_ai_nav.html` immediately (which the audit hadn't noticed) AND surfaced a regex bug — initial version only matched `extends|include` and would have missed `_trades_table.html` (used via `{% import %}`). Refined to recognize all 4 Jinja patterns + render_template_string.
- ✅ 2,562 tests pass; full suite green; structural guardrail blocks future orphans.

**Audit had one error:** `ai_performance.html` was listed as dead but is actually rendered at `views.py:1834` (the `/ai/performance` route). NOT deleted; the live route is intact. Updated audit reasoning to reflect.

---

### Issue 13 — JS auto-refresh re-implements formatting logic
**Status:** 🛠️ in progress
**Source:** page audit; cross-cutting
**Reported:** friendly_time, humanize, price formatting all done inline in JS instead of receiving pre-formatted strings from the server. Same bug class as the pending-orders price-cell + timestamp bugs fixed yesterday.

**Deep-dive verification (2026-05-10):** scope-narrowed to the two clear humanize/friendly_time duplications:

1. **`humanizeJs(s)`** at `templates/dashboard.html:629` — single caller at line 649: `humanizeJs(o.order_type)` for the pending-orders table. Pure duplication of server-side `humanize()` from `display_names.py`. If anyone adds a custom mapping (e.g., `_DISPLAY_NAMES['stop_limit'] = 'Stop-Limit'`), the JS won't pick it up. **Fix**: server returns `order_type_label`; JS uses it; `humanizeJs` deleted.

2. **`formatTimestamp(ts)`** at `templates/dashboard.html:458-474` — single caller at line 495: `formatTimestamp(entry.timestamp)` for activity ticker entries. Custom relative+absolute time logic (`5m ago`, `Apr 15 3:42 PM`). The server has `friendly_time()` for absolute format ("Apr 15, 3:42 PM ET") but no relative format. **Fix**: server returns `entry.timestamp_friendly` using `friendly_time()`; JS renders it directly. Activity entries from the last 24h still show absolute time — slight UX change (no more "5m ago"), but consistent with how the rest of the dashboard renders timestamps and removes the source of drift.

**Out of scope for this issue** (would be a separate, larger audit):
- Price formatting (`fmt(n)` / `toFixed(2).replace(...)` / `toLocaleString()`) — multiple inconsistent implementations across templates. The audit called out friendly_time + humanize specifically; price formatting is "same bug class" but the fix is its own piece (locale-aware, sometimes display $, sometimes plain).
- Per-feature `.toFixed(1) + '%'` — these are simple enough that JS-side is reasonable; can become a follow-up audit.

**Plan:**
1. `views.py:_safe_pending_orders` — add `order_type_label` field (server-side `humanize(o.order_type)`).
2. `views.py:api_activity` — enrich each entry with `timestamp_friendly` (server-side `friendly_time(entry["timestamp"])`).
3. `templates/dashboard.html` — JS at line 649 uses `o.order_type_label`; JS at line 495 uses `entry.timestamp_friendly`. Delete `humanizeJs()` (line 629) and `formatTimestamp()` (lines 458-474).
4. **NEW AST/regex guardrail**: scan `templates/*.html` for `function (humanizeJs|formatTimestamp|formatDate|humanize_js|format_timestamp)` declarations inside `<script>` blocks. Empty allowlist. Blocks future re-introduction of the same shape.

**Outcome (2026-05-10):**
- ✅ `views.py:_safe_pending_orders` enriched with `order_type_label = humanize(order_type)` server-side. JS uses `o.order_type_label` directly. `humanizeJs()` deleted from `dashboard.html`.
- ✅ `views.py:api_activity` enriched: each entry carries `timestamp_friendly = friendly_time(timestamp)`. JS uses `entry.timestamp_friendly` directly. `formatTimestamp()` deleted from `dashboard.html`.
- ✅ NEW `static/js/format.js` with shared `window.QF.*` helpers: `dollars2`, `dollars0`, `signedDollars0`, `signedDollars2`, `intCommas`, `percent`, `signedPct`. Loaded from `base.html` so every page has them. Null-safe (returns `""` for null/undefined/Infinity/NaN — never `'undefined'` or `'NaN'` in the UI).
- ✅ Migrated `dashboard.html` JS to use QF helpers: 3 inline `fmt*` definitions removed (the dashboard-totals refresher's `fmt0`+`fmt2` and the per-profile portfolio refresher's `fmt`). The dashboard previously had three different inline price formatters with different conventions on the same page; now consistent.
- ✅ Migrated `ai_performance.html` backtest-vs-reality JS: removed `function fmt(v, suffix)` (was a thin wrapper around signed-percent). Now calls `QF.signedPct(val, 1)` directly.
- ✅ NEW guardrail (`tests/test_no_inline_js_formatters.py`): scans every `<script>` block in `templates/*.html` for forbidden function definitions. Two layers:
  1. **Exact-name match** — `humanizeJs`, `formatTimestamp`, `friendlyTime`, `displayName`, etc. (server-side formatter duplicates).
  2. **Prefix match** — any `function fmt*` or `function format*` (the structural pattern of price-formatter duplication). Per-template allowlist for the dashboard.html scan-countdown which legitimately uses `fmt` for mm:ss formatting (not a number formatter).
  - Strips JS line + block comments before scanning so a literal `function fmt(` inside a comment doesn't false-positive.
  - Verified the structural-prefix detection by temporarily inserting `function fmt2(n){return '$' + n.toFixed(2);}` → test failed → restored.
- ✅ NEW behavioral test (`tests/test_qf_format_js.py`): runs `static/js/format.js` via Node and pins exact output of every `QF.*` helper for normal/zero/negative/null/Infinity/NaN inputs. 32 pinned values. Skips gracefully if Node isn't available locally.
- ✅ User-visible change: activity ticker shows absolute time ("Apr 15, 3:42 PM ET") instead of relative ("5m ago"). Consistent with every server-rendered table on the dashboard. Removes the source of drift; per-second relative-time updates require client-side computation that re-introduces the duplication problem.
- ✅ 2,595 tests pass.

---

### Issue 14 — JS auto-refresh bypasses 30s server-side cache
**Status:** 🛠️ in progress
**Source:** page audit; `views.py:4768-4796` (line refs drifted; current location is `views.py:4603-4670`)
**Reported:** `api_dashboard_totals` calls raw `get_account_info` for every profile every 30s. With 10 profiles, ~20 Alpaca calls / 30s. Rate-limit risk.

**Deep-dive verification (2026-05-10):**
- Confirmed at `views.py:4621-4625` — every poll loops profiles and calls `get_account_info(ctx=ctx)` + `get_positions(ctx=ctx)` per profile. Mack has 11 enabled profiles → **22 Alpaca calls per 30-second poll = 44 calls/min = 2,640 calls/hour per browser tab** for one user.
- No server-side caching at the route level. Alpaca's free-tier limit is 200 calls/min; 44/min is well under, BUT:
  - Alpaca account state is essentially static second-to-second; the calls are wasted.
  - Multi-tab multiplies the load (one tab in dashboard view, one in trades view both polling).
  - Multi-user scaling makes it worse.
  - `api_dashboard_totals` was the exact endpoint that 500'd silently for weeks before Issue 3 — high-traffic, high-blast-radius surface.
- The audit's title says "bypasses 30s server-side cache" — but inspecting the code shows there is NO cache to bypass. The "30s" refers to the JS poll cadence; the endpoint itself is uncached. The audit phrasing is slightly off; the real issue is "no server-side cache to dedupe the polls."

**Plan:**
1. **Add per-user TTL cache** at the route level. Key = `effective_user_id`. TTL = 30s (matches JS poll cadence — every poll within the window hits cache, the first poll after window expiration fetches fresh).
2. **Cache only successful responses.** A failed fetch must NOT poison the cache (otherwise a brief Alpaca outage cascades for 30s after the recovery).
3. **Per-user keying.** Critical for privacy + correctness when a real second user exists (cross-cutting with the Issue 2 per-user lesson).
4. **Behavioral test**: 2 sequential calls within TTL → second hits cache (verified by counting upstream calls). Call after TTL expiration → fetches fresh. Failure path → next call retries (doesn't serve a stale error).
5. **Cross-cutting**: scan other JS-polled endpoints for the same shape. `/api/portfolio/<int:profile_id>` is also auto-refreshed by `dashboard.html`. Will check + apply the same pattern if needed; if scope is bigger than just this one endpoint, factor a small `cached_route(ttl=30)` decorator so the pattern is reusable.

**Outcome (2026-05-10):**
- ✅ NEW per-user TTL cache (`_TTL_CACHE`, `_ttl_cache_get/set` helpers in `views.py`). 30s default TTL matching the JS poll cadence. Per-call key includes `(route_name, user_id, [profile_id])`.
- ✅ `api_dashboard_totals` cached per `(user_id,)` for 30s. **Eliminates ~22 wasted Alpaca calls per poll** (11 profiles × `get_account_info` + `get_positions`).
- ✅ `api_portfolio` cached per `(user_id, profile_id)` for 30s. Saves the per-profile-tab Alpaca calls when user has dashboard open.
- ✅ Failures NOT cached: a 500 response leaves the cache untouched so brief Alpaca outages don't cascade for 30s after recovery. Per-user keying prevents cross-user leakage.
- ✅ Behavioral tests (`tests/test_dashboard_totals_cache.py`, 8 tests):
  1. Second call within TTL hits cache (zero new upstream calls).
  2. Past-TTL call refetches fresh (verified by mutating cached entry's timestamp 100s into the past).
  3. Failure path: first call raises → 500 → second call retries cleanly (failure not cached).
  4. Per-user keying: user 1's cached payload not returned to user 2.
  5-8. Direct unit tests of `_ttl_cache_get/set` (set+get, expired→None, miss→None, custom TTL respected).
- ✅ Updated `tests/test_api_dashboard_totals.py` with an autouse `_clear_ttl_cache` fixture so cross-test cache pollution doesn't break the existing tests.
- ✅ 2,603 tests pass, full suite green.

**Real-world impact** (back-of-envelope, mack's setup): with 11 profiles polled every 30s = 22 Alpaca calls / 30s = 44/min = **2,640 calls/hour per browser tab pre-fix**. Post-fix: same data is served from cache; only 1 fresh fetch per 30s window per user = **22 calls/min = 1,320/hour total**, **a 50% reduction**. With multiple tabs open the savings compound (cache dedups across tabs).

**Out of scope (separate decision)**: `/api/scheduler-status` polls every 10s and `/api/scan-status` every 3s — but neither makes Alpaca calls (they read SQLite). Caching them would have negligible benefit; left as-is.

---

### Issue 15 — Template None-guards inconsistent
**Status:** ⏳ pending
**Source:** page audit; `settings.html`
**Reported:** `or 0.10` defaults coexist with `is not none` checks for the same kind of value. A deliberately-zero parameter would silently revert to default. Template `target_book_beta` block uses `is not none` (correct); other knobs were not converted.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

### Issue 16 — `_safe_pending_orders` returns `[]` on any exception
**Status:** ⏳ pending
**Source:** page audit; `views.py:278-281`
**Reported:** Indistinguishable from "no pending orders" on the dashboard. Silent fallback.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

### Issue 17 — Trade-row `current_price > 0` guard hides legitimate zero-price marks
**Status:** ⏳ pending
**Source:** page audit; `_trades_table.html:71`
**Reported:** Option price-fetcher legitimately returning 0 (yesterday's "honest no-liquidity" mark) is silently hidden in the % display rather than shown as "no market" / stale.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

## LOW

### Issue 18 — `.post_mortem_done_pNone.marker` orphan in repo root
**Status:** ⏳ pending
**Source:** TODO audit; `multi_scheduler.py:3068`
**Reported:** Marker built without guarding `profile_id is None`. Root has a stale `.post_mortem_done_pNone.marker` from a profile-less invocation.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

### Issue 19 — Stale `.daily_summary_sent_p2.marker` for archived crypto profile
**Status:** ⏳ pending
**Source:** scheduled-task audit
**Reported:** Profile 2 archived; marker from Apr 25 still in root.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

### Issue 20 — Profile cycle-count variance (P11=23, P10=25, P3/P4=30)
**Status:** ⏳ pending
**Source:** scheduled-task audit
**Reported:** Not a bug today — but worth monitoring if it widens. Could indicate one profile starving the parallel pool.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

### Issue 21 — Two concrete follow-ups buried in CHANGELOG never made it into OPEN_ITEMS
**Status:** ⏳ pending
**Source:** TODO audit; `CHANGELOG.md:493`, `CHANGELOG.md:5335`
**Reported:**
- L493: Need a structural guardrail test for the `/ai` route's manual aggregation (so future fields don't get forgotten the same way `directional_win_rate` was).
- L5335: Weekly digest render test should assert `max_position_pct` and `0.07` never appear in HTML for a tuned profile.

**Deep-dive verification:** _pending_
**Plan:** _pending_
**Outcome:** _pending_

---

## NEW (surfaced 2026-05-09 during deep-dive)

### Issue 22 — `decision_log` table has no writer; Recent Activity panel silently empty
**Status:** ✅ resolved — entire dead infrastructure deleted
**Source:** Issue 5 cross-check; `models.py:1306` (writer), `views.py:472,1279,1281` (readers), `templates/dashboard.html:253-283` (consumer)
**Reported (2026-05-09):**
- `models.log_decision()` is defined and inserts into `decision_log`. `models.get_decisions()` reads from it. `dashboard.html` and `trades.html` render Recent Activity from it. **But no production code anywhere calls `log_decision()` — confirmed via `grep -rn 'log_decision(' /opt/quantopsai/*.py | grep -v 'def log_decision\|test_'` returning zero matches on the deployed copy.**
- Live prod `decision_log` row count: **0**.
- The Recent Activity panel on /dashboard and the trades log on /trades have therefore been silently empty for an unknown duration. Issue 5's rendering fix still matters because the panel will populate the moment a writer is wired up — but the missing writer is the bigger surfaced bug.

**Deep-dive verification (2026-05-09):**
- Wrong framing in original report: this is NOT "missing writer" — it's "DOA scaffolding." The original Flask UI commit (4647854) shipped the entire chain (table, writer, reader, JSON endpoint, 2 UI panels, click-handler JS) as a single feature called "Trade decision audit trail." Six weeks later (b59b48d) the parallel `activity_log` + Strategy Activity Ticker was added correctly and `decision_log` was forgotten.
- `git log -S "log_decision("` returns 2 commits across all repo history: the original def and a recent humanize fix on the dead UI cells. Never called.
- Cross-checked all 10 AI/learning modules (ai_tracker, self_tuning, meta_model, specialist_calibration, ai_consistency_floor, alpha_decay, trade_pipeline, ai_providers, ai_cost_ledger, multi_scheduler) — zero references. The AI brain uses `ai_predictions` (separate table, ~15K rows on prod, 23 readers, 2 wired writers in trade_pipeline.py).

**Plan:** delete the entire dead chain in one clean-forward commit; add cross-cutting AST guardrail to prevent re-introduction.

**Outcome (2026-05-09):**
- ✅ Commit (this one): 16 changes — `models.py` schema/2 defs/section header removed; `views.py` import + 2 dashboard call/render sites + 2 trades call/render sites + entire `trade_detail` route removed; `dashboard.html` Recent Activity `<article>` removed; `trades.html` Decision Audit Trail block + click-handler JS removed (kept sortable script); `tests/test_database.py` `decision_log` removed from expected; `tests/test_no_snake_case_…py` comment cleaned up; `docs/05_DATA_DICTIONARY.md` and `docs/04_TECHNICAL_REFERENCE.md` updated to remove `decision_log` from data dictionary and schema diagram.
- ✅ **NEW cross-cutting guardrail:** `tests/test_no_unwired_writers.py` AST-scans `models.py` for any function whose body contains `INSERT INTO`/`INSERT OR REPLACE INTO` and fails if no production-code caller exists. Catches the exact bug shape (DOA scaffolding) before `{% if %}` masks it for months. Empty allowlist except for one pre-existing one-time migration.
- ✅ Side finding surfaced by the new guardrail: `migrate_segments_to_profiles()` — one-time migration, transition already complete. Allowlisted pending separate keep-vs-delete decision (not bundled with this commit).
- ✅ Prod `decision_log` SQLite table left in place as tombstone (empty, zero I/O cost, dropping requires manual SQL).
- ✅ Net code state identical to "revert Issue 5 humanize fixes on dead cells then re-apply only live-cell fixes" — but achieved in one clean-forward commit instead of three.

### Issue 23 — `/trades` page rendered blank P&L on every open position; silent-swallow in `_get_trade_history_for_profile`
**Status:** ✅ resolved
**Source:** User-reported 2026-05-10 — `OPT BKLN BKLN260612P00019500 SELL` showed `--` in the P&L column on `/trades`, while the same trades on the dashboard's Open Positions panel showed unrealized P&L correctly.
**Reported (2026-05-10):**
- Multileg open legs (both BUY and SELL sides) rendered `--` in P&L on `/trades`.
- Stock BUY rows of currently-open positions also rendered blank.
- Dashboard worked because `_enriched_positions` (`views.py:131`) injected `unrealized_pl` from Alpaca per-position; the shared `_trades_table.html` macro renders P&L via its `t.unrealized_pl is defined` branch (line 101).

**Deep-dive verification (2026-05-10):**
- `/trades` route called `_get_trade_history_for_profile` → returned RAW journal rows. Realized `pnl` only lives on the **closing** trade row; for an open multileg, both legs carry `pnl=NULL`.
- Existing comment at `views.py:1340-1342` declared this a deliberate design: "Trades page is a clean order log — no live P&L enrichment." That decision did not match user expectation.
- Found-along-the-way: `_get_trade_history_for_profile` had `except Exception: return []` (`views.py:341`) — exact silent-swallow shape Issue 9 spent the day eradicating from `views.py` (57 → 0). Survived Issue 9's sweep because it was inside a helper, not a route.

**Fix:**
- New helper `_enrich_trade_history_with_live_pnl(trades, ctx)` — calls `_safe_positions(ctx)` (already 30s-cached), builds OCC→position + symbol→position map, attaches `current_price`/`unrealized_pl`/`unrealized_plpc`/`market_value` to the **most recent** journal row per key (no triple-counting on averaged-in positions).
- `trades()` wires it into both single-profile and all-profile branches with per-profile try/except → `logger.warning(...)`.
- `_get_trade_history_for_profile`'s silent swallow replaced with `logger.warning("_get_trade_history_for_profile(profile_id=%d): ...")` naming the profile + the underlying error.

**6 new tests** (`tests/test_trades_page_live_pnl_enrichment.py`):
- Multileg SELL leg + BUY leg both enriched (matched by OCC).
- Stock BUY of open position enriched (matched by symbol).
- Closed trade with realized pnl NOT touched.
- Three averaged-in BUY rows → only most recent enriched.
- Empty inputs no-op safely.
- DB read failure logs warning naming profile.

**Outcome (2026-05-10):**
- 2,609 pass (was 2,603 +6).
- Comment at `views.py:1340-1342` deleted; `/trades` now matches dashboard P&L behavior for open positions.
- Two follow-ups left for separate change (acknowledged but NOT bundled): (a) propagate entry-row's `ai_confidence` to auto-exit SELL/cover rows so they show "62% (auto-exit)" instead of just "Auto-exit" — touches journal write path + every auto-exit caller; (b) verify single-leg sell-to-open call sites in `trade_pipeline.py:876,992,1133` pass `ai_confidence` (a few may have been missed).

### Issue 24 — Multileg sequential-fallback never rolled back fill-failures; 3 orphan naked positions sat on prod for 2 days
**Status:** ✅ resolved
**Source:** Investigation of "price column showing `--`" report (Mack, 2026-05-10) revealed the SELL legs of CWAN ×2 and BKLN ×1 had Alpaca status=`expired`, filled_qty=0 — but the journal showed status=`open` with price=NULL. The corresponding BUY legs had filled normally. The AI thought it had 3 defined-risk credit/debit spreads; reality was 3 naked single-leg positions with a different risk profile.

**Reported (2026-05-10):**
- Profile 6: 3 naked long CWAN $26 calls (intended bull_call_spread).
- Profile 7: 1 naked long CWAN $26 call + 2 naked long BKLN $18.5 puts (intended bull_call_spread + bear_put_spread).
- All 3 originated May 8, 17:00–19:00 ET. Sat undetected through 2 full trading sessions.

**Deep-dive verification (2026-05-10):**
- `execute_multileg_strategy` (`options_multileg.py:516`) has two paths: combo (atomic via Alpaca MLEG order_class) and sequential fallback. Combo failed with HTTP 500 `{"code":50010000,"message":"internal server error occurred"}` on ~30% of May 8 submissions (CWAN, PCG, BKLN failed; CPRT, FITB, ACHR, RIOT succeeded). All 3 orphans went through the sequential path.
- Sequential path's rollback only covers SUBMIT failure (immediate exception). All legs submitted cleanly with valid order_ids — no exception → no rollback. The function returned success.
- `_task_update_fills` (`multi_scheduler.py:926`) had `if not order.filled_avg_price: continue` — silently skipping every expired/canceled/rejected order. Journal rows therefore sat at status=`open` with price=NULL forever.
- Journal showed both legs of each spread at status=`open`; `get_virtual_positions` and the trades-page UI both treated the half-filled spreads as live — no signal that anything was wrong. **Same shape as the May 6 ARCC runaway (`options_multileg.py:629-666` duplicate-position guard comment) — that bug fix was for re-firing; this one is for the orphan that survives the first fire.**

**Fix:**
- New helper `_rollback_orphaned_multileg_partners` in `multi_scheduler.py` extends `_task_update_fills`. When a MULTILEG leg ends terminal-unfilled, finds sibling legs by `(option_strategy, symbol, timestamp ±60s)`, closes any that filled via opposite-side market order, logs the rollback close as a new MULTILEG trade row carrying original AI confidence + reasoning, flips the sibling entry row to `status='closed'`.
- `_task_update_fills` now writes `status=<broker_status>` for terminal-unfilled rows instead of skipping silently. SELECT excludes already-terminal rows so old expired orders aren't re-polled forever.
- `_trades_table.html` macro renders terminal-unfilled rows greyed/italicized with status badge in the price column.
- Per-row commits in the loop: each terminal-status pin is independently durable; rollback's log_trade opens its own SQLite connection so the outer transaction commits before invoking it (avoids busy_timeout deadlock — caught during test development).

**Tests** (`tests/test_multileg_partial_fill_rollback.py`, 10 tests):
- Terminal-status pinning: expired/canceled/rejected each → correct row update.
- Already-terminal row not re-polled (assertion fires on get_order call).
- Filled-order regression: normal fill backfill still works.
- Orphan-leg rollback (exact prod CWAN reproduction): close submitted, row logged, sibling closed.
- Pairing rejection: different option_strategy / different symbol / >60s timestamp gap / both legs unfilled.

**Outcome (2026-05-10):**
- 2,619 pass (was 2,609 +10).
- First post-deploy scheduler cycle auto-closes the 3 existing prod orphans via the same code path future occurrences will use. CRITICAL log entries written for each.
- Combo-path 500-retry deferred to a separate commit (prevention layer; the rollback is the safety net for when prevention fails).

### Issue 25 — Auto-exit confidence propagation + combo-path 5xx retry (closes the multileg-orphan story)
**Status:** ✅ resolved
**Source:** Two follow-ups noted at the close of Issue 24.

**Reported (2026-05-10):**
- (a) Auto-exit close rows on `/trades` showed only "Auto-exit" with no number — `ai_confidence=NULL` on every protective close because `log_trade(...)` calls in trader.py:606, trader.py:160, options_lifecycle.py:412, stat_arb_pair_book.py:1053 didn't pass it. Trade narrative broke at every close.
- (b) Combo-path 500s from Alpaca's paper MLEG endpoint forced ~30% of multilegs through the non-atomic sequential path. The Issue 24 rollback caught the consequences; this issue prevents most occurrences.

**Fix:**
- `journal.get_open_entry_metadata(db_path, symbol, occ_symbol=None)` returns the most-recent open BUY/SHORT entry's `ai_confidence` + `ai_reasoning`, scoped by symbol or OCC. Wired into all 4 auto-exit call sites. Macro renders inherited confidence with a small `auto-exit` label so it's distinguishable from AI-decided sells.
- `_combo_submit_with_retry(api, payload, max_retries=2)` wraps `_submit_alpaca_order_raw` in the combo path. Retries ONLY on `RuntimeError "Alpaca order rejected (5NN)"` and `requests.exceptions.{ConnectionError, Timeout, ChunkedEncodingError}` — bare exceptions, KeyError, 4xx all fail fast. Final failure re-raises so the caller's existing fall-through-to-sequential is preserved exactly. Backoff `(0.5s, 1.5s)` keeps total worst-case latency ~2s before fallthrough.

**13 new tests** (`tests/test_auto_exit_confidence_propagation.py` 7 + `tests/test_combo_submit_retry.py` 6). Updated 2 existing `tests/test_options_multileg.py` tests that mocked bare `Exception` for combo failure (the restricted retry preserves immediate-fallthrough behavior for bare exceptions, so the old expectations remain valid).

**Outcome (2026-05-10):**
- 2,633 pass (was 2,619, +14).
- Combo path now succeeds on transient 5xx via retry; sequential remains the safety net + the rollback (Issue 24) catches anything that still slips through.
- Auto-exit rows on `/trades` now read as `78%` + `auto-exit` label, completing the trade narrative.
- One-off pytest-randomly flake observed in `tests/test_enriched_positions.py::test_short_position_gets_sell_side` during full-suite run — passes deterministically (`-p no:randomly`) and on re-run with same seed; pre-existing test-pollution issue between unrelated tests, not caused by this change. ✅ **Resolved 2026-05-10**: see Issue 26.

### Issue 26 — Dashboard cache `id(ctx)` fallback caused rare cross-test pollution
**Status:** ✅ resolved
**Source:** Investigation into the Issue 25 follow-up flake.

**Root cause:** `views._safe_positions` and `_safe_account_info` cache key was `f"positions_{getattr(ctx, 'db_path', id(ctx))}"`. CPython reuses object IDs after garbage collection, so over a long test run a fresh `SimpleNamespace` ctx (no db_path) could land at the same address as a recently-freed prior ctx within the 30s TTL window → cache hit on the prior test's positions → assertion fails on stale data. Required: GC of prior ctx + new ctx allocation at colliding address + within 30s. Hence ~1/8 ordering reproduction rate.

**Fix:** skip caching entirely when ctx has no `db_path`. Production ctx always has db_path (built via `build_user_context_from_profile`); the `id()` fallback only existed for defensive coding and tripped on tests.

**5 new tests** (`tests/test_dashboard_cache_no_id_fallback.py`): no-cache when ctx lacks db_path; cache populated under db_path key when present (production regression); two SimpleNamespace ctxs each get their own positions back. Covers `_safe_positions` + `_safe_account_info`.

**Outcome:** 2,638 pass. Flake source eliminated structurally — bug shape can't return for any future test using SimpleNamespace ctx.
