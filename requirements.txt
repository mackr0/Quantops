alpaca-trade-api==3.2.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
ta>=0.11.0
anthropic>=0.39.0
openai>=1.0.0
google-generativeai>=0.3.0
rich>=13.0.0
yfinance>=1.2.0
flask>=3.0.0
flask-login>=0.6.0
bcrypt>=4.0.0
cryptography>=42.0.0
gunicorn>=22.0.0
praw>=7.7.0
scikit-learn>=1.3.0
# Item 1b — stat-arb pair book. statsmodels provides Engle-Granger
# cointegration test and ADF stationarity test. Used by
# stat_arb_pair_book.py to find / monitor cointegrated equity pairs.
statsmodels>=0.14.0
# Item 3a — Google Trends attention signal (alternative_data.get_google_trends_signal).
pytrends>=4.9.0
# pytest-randomly is dev-only but we keep it in requirements so CI
# and local dev both default to randomized test ordering. This
# catches order-dependent test pollution at the moment it's
# introduced rather than as a one-off flake later.
pytest-randomly>=3.15.0
pytest>=8.0.0
pytest-timeout>=2.0.0
# altdata/ scrapers (congresstrades, edgar13f, biotechevents, stocktwits).
# click + beautifulsoup4 + lxml are already pulled transitively by
# rich / yfinance respectively — declaring them explicitly because the
# altdata code imports them directly. pdfplumber is the only genuinely
# new addition (congresstrades parses House Clerk PTR PDFs).
click>=8.1
beautifulsoup4>=4.12
lxml>=5.1
pdfplumber>=0.10
